{
  "name": "gestion-des-parcs-informatique",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "pip install -r requirements.txt && npm --prefix frontend install",
    "dev": "concurrently -k -n API,WEB -c cyan,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:web": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
